import 'package:flutter/material.dart';

import '../widgets/SummaryDataWidget.dart';

class SummaryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: SummaryDataWidget(),
      );

  }
}

